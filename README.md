# Basic-scripts-and-date-base-for-TG-bots
Базовые скрипты и структура базы данных для быстрого старта в использовании БД SQLite3, а также использование БД SQLite3 в Telegram-ботах.

---

# 🐍 Python + SQLite3: От основ до реальных проектов

**Мои практические наработки по SQLite3 в Python: от простых скриптов до Telegram-ботов с базой данных.**

Этот репозиторий содержит коллекцию моих рабочих проектов, где я применял SQLite3 в разных контекстах. Здесь вы найдете эволюцию моих навыков — от первых экспериментов с БД до создания полноценных приложений.

## 📚 Содержание

### 1. **Основы SQLite3** (`basic_sqlite.py`)
Мои первые шаги в работе с базами данных:
- Подключение/создание БД
- Создание таблиц с разными типами данных
- Базовые CRUD-операции
- Методы выборки: `fetchall()`, `fetchone()`, `fetchmany()`
- Работа с датами в SQLite

```python
# Мой первый эксперимент с SQLite
db = sqlite3.connect('ZTTao.db')
c = db.cursor()
c.execute("""CREATE TABLE articles (
    title text,
    full_text text,
    views integer,
    avtor text
)""")
```

### 2. **Консольный менеджер задач** (`task_manager.py`)
Полноценное CRUD-приложение с продуманным интерфейсом:
- ✅ Добавление задач с приоритетами и статусами
- 📋 Просмотр всех задач с фильтрацией
- 🔄 Изменение статусов
- ❌ Удаление с подтверждением
- 🚪 Удобное консольное меню

**Особенности реализации:**
- Проверка ввода (`isdigit()` для ID)
- Возможность отмены через `stop`
- Защита от SQL-инъекций
- Автоинкремент ID

### 3. **Кинотеатр + отзывы** (`cinema_reviews.py`)
Система учета фильмов с отзывами:
- 🎬 Таблица фильмов с рейтингами
- 💬 Таблица отзывов пользователей
- 🔗 JOIN-запросы для связи данных
- 📊 Автоматический пересчет рейтингов

```python
# Автоматическое обновление рейтинга фильма
def update_rating(movie_id):
    c.execute("SELECT rating FROM reviews WHERE movie_id = ?", (movie_id,))
    ratings = c.fetchall()
    if ratings:
        avg = sum(r[0] for r in ratings) / len(ratings)
        c.execute("UPDATE movies SET avg_rating = ? WHERE id = ?", (avg, movie_id))
```

### 4. **🏋️ Telegram-бот: Трекер тренировок** (`workout_tracker_bot.py`)
**Реальный проект с использованием:**
- `python-telegram-bot` для взаимодействия
- SQLite для хранения данных
- Inline-кнопок для удобного интерфейса
- Машины состояний через `register_next_step_handler`

**Функционал бота:**
- ▶️ **Начать тренировку** — записывает тип тренировки и время начала
- ⏹️ **Закончить тренировку** — автоматически считает продолжительность
- 📊 **История** — показывает статистику по всем тренировкам
- 🏠 **Главное меню** — интуитивная навигация

**Структура БД для бота:**
```sql
CREATE TABLE workout (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    name TEXT,
    training TEXT,
    start_time TEXT,
    end_time TEXT,
    minutes REAL,
    workout_date TEXT DEFAULT (DATETIME('now', 'localtime'))
)
```

## 🎯 Чему можно научиться из этих примеров?

### 📈 **Эволюция сложности:**
1. **Уровень 1:** Простые запросы (`INSERT`, `SELECT`)
2. **Уровень 2:** Консольное приложение с меню
3. **Уровень 3:** Связь нескольких таблиц (фильмы + отзывы)
4. **Уровень 4:** Интеграция с Telegram API

### 🔧 **Технические навыки:**
- **SQL-запросы:** От простых до сложных с `JOIN`
- **Безопасность:** Параметризованные запросы (`?`)
- **Архитектура:** Организация кода для масштабирования
- **Работа с датами:** `datetime.strptime()`, форматирование
- **Обработка ошибок:** Проверка ввода, обработка `None`

### 💡 **Практические паттерны:**
```python
# 1. Безопасное подключение к БД
db = sqlite3.connect('file.db', check_same_thread=False)

# 2. Проверка существования записи
cursor.execute("SELECT * FROM table WHERE id = ?", (id_value,))
record = cursor.fetchone()
if record is None:
    print("Запись не найдена!")

# 3. Обновление с вычислением
start_time = datetime.strptime(start_str, "%d-%m-%Y, %H:%M")
end_time = datetime.now()
minutes = (end_time - start_time).seconds // 60
```

## 🚀 Быстрый старт

### Для изучения основ:
```bash
# Последовательно запускайте файлы:
python basic_sqlite.py      # Основы SQLite
python task_manager.py       # Консольное приложение
python cinema_reviews.py     # Работа с несколькими таблицами
```

### Для запуска Telegram-бота:
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и сохраните в `config.py`:
   ```python
   TOKEN = 'ваш_токен_здесь'
   ```
3. Установите зависимости:
   ```bash
   pip install pyTelegramBotAPI
   ```
4. Запустите бота:
   ```bash
   python workout_tracker_bot.py
   ```

## 📁 Структура знаний

```
Мои проекты SQLite3/
├── 📘 Основы (Уровень 1)
│   └── basic_sqlite.py
├── 📙 Консольные приложения (Уровень 2)
│   ├── task_manager.py
│   └── cinema_reviews.py
├── 📗 Telegram-боты (Уровень 3)
│   └── workout_tracker_bot.py
└── 📚 Общие концепции:
    ├── Подключение/закрытие БД
    ├── Создание таблиц
    ├── CRUD-операции
    ├── JOIN-запросы
    └── Интеграция с внешними API
```

## 🔍 Ключевые SQL-запросы из проектов

### Из трекера тренировок:
```sql
-- Найти незавершенную тренировку пользователя
SELECT id, training, start_time 
FROM workout
WHERE user_id = ? AND end_time IS NULL
ORDER BY id DESC
LIMIT 1

-- Обновить время окончания и продолжительность
UPDATE workout 
SET end_time = ?, minutes = ? 
WHERE id = ?
```

### Из системы кинотеатра:
```sql
-- Получить все отзывы с названиями фильмов
SELECT reviews.id, reviews.reviewer, reviews.rating, reviews.comment, movies.title
FROM reviews
JOIN movies ON reviews.movie_id = movies.id
ORDER BY reviews.id
```

## 🎓 Мой путь обучения

> "Я начал с простых экспериментов (`basic_sqlite.py`), чтобы понять, как работают базы данных. Потом создал консольные приложения для закрепления знаний. Когда основы были освоены, я перешел к созданию Telegram-бота — это потребовало совместить знания SQLite с асинхронным программированием и обработкой callback-запросов."

**Хронология:**
1. ✅ Изучил базовый синтаксис SQLite
2. ✅ Создал консольные CRUD-приложения
3. ✅ Освоил работу с несколькими связанными таблицами
4. ✅ Интегрировал БД в Telegram-бота
5. 🔜 Планирую: PostgreSQL, асинхронные запросы, миграции

## 💼 Где это можно применить?

- **Учебные проекты** — готовые примеры для студентов
- **Прототипирование** — быстрый старт для MVP
- **Личные проекты** — боты для учета тренировок, привычек, задач
- **Портфолио** — демонстрация навыков работы с БД

---

## 🏆 Выводы

**Из этих проектов видно, что SQLite — мощный инструмент, который можно использовать для:**
1. **Быстрого прототипирования** (нет необходимости в настройке сервера БД)
2. **Небольших проектов** (Telegram-боты, десктопные приложения)
3. **Обучения** SQL и работе с базами данных

**Мои главные инсайты:**
- Всегда использовать параметризованные запросы
- Закрывать соединения с БД (`db.close()`)
- Документировать сложные SQL-запросы в комментариях
- Разделять код на функции для удобства поддержки

---

> *"Сложные проекты начинаются с простых примеров. Эти файлы — моя личная история освоения SQLite, от первых строчек кода до работающего Telegram-бота. Так же это моя личная памятка для помощи в работе, в моем GitHub вы можете увидеть такие же проекты, которые нужны лично для моих задач."*
---
